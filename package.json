{
  "name": "lonely-wiki",
  "version": "0.1.0",
  "description": "A personal offline Wiki application with Markdown support and tag-based organization",
  "author": "Lonely Wiki Contributors <contact@example.com>",
  "private": true,
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "test:imports": "node test-imports.js",
    "test:modules": "node test-module-loading.js",
    "test:init": "node test-initialization.js",
    "test:asar": "node test-asar-structure.js",
    "test": "npm run test:imports && npm run test:init",
    "dev": "concurrently \"npm run dev:vite\" \"wait-on http://localhost:5173 && electron .\"",
    "dev:vite": "vite",
    "db:init": "node src/db/init.js",
    "build": "npm run lint && npm run test:imports && npm run test:init && npm run db:init && npm run build:vite && npm run build:electron",
    "build:vite": "vite build",
    "build:electron": "electron-builder --publish never",
    "build:win": "npm run lint && npm run test:imports && npm run test:init && npm run db:init && npm run build:vite && electron-builder --win --publish never",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^17.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sqlite3": "^5.1.7",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@vitejs/plugin-react": "^4.2.1",
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "electron-builder": "^24.6.4",
    "eslint": "^9.39.2",
    "vite": "^5.0.8",
    "wait-on": "^7.0.1"
  },
  "build": {
    "appId": "com.lonely-wiki.app",
    "productName": "Lonely Wiki",
    "files": [
      "dist/**/*",
      "src/**/*",
      "node_modules/**/*"
    ],
    "directories": {
      "buildResources": "assets",
      "output": "release"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "certificateFile": null,
      "certificatePassword": null
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Lonely Wiki"
    }
  }
}
